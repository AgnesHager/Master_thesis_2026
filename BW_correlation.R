# Checking to see if gene X is associated with Delta BW (so body weight gain)
# Using the DEGs as genes I correlate to
# Using Spearman correlation, to account for non-normality and potential outliers

